# テストシナリオ一覧

**プロジェクト**: ThinkTool  
**最終更新**: 2026-03-27  
**テストフレームワーク**: Vitest（unit/ipc）、Playwright（e2e）

---

## 実行コマンド

| コマンド | 対象 | 所要時間 |
|---|---|---|
| `npm test` | unit + ipc（全 155 件） | 約 5 秒 |
| `npm run test:coverage` | unit + ipc + カバレッジ出力 | 約 6 秒 |
| `npm run test:e2e` | e2e（ビルド後に Electron 起動） | 約 1〜2 分 |
| `npm run test:e2e:ui` | e2e（Playwright UI モード） | インタラクティブ |

---

## 1. ユニットテスト — `tests/unit/treeOps.test.ts`

対象: `src/lib/treeOps.ts`（純粋関数ライブラリ）  
環境: Node.js（DOM 不要）  
件数: **38 件**

---

### 1-1. `toggleNode` — ノード展開状態の切り替え（4 件）

| # | テスト名 | 期待結果 |
|---|---|---|
| 1 | ルート直下ノードの expanded を反転する | `expanded` が `true` になる |
| 2 | 再度呼ぶと元に戻る | `expanded` が `false` に戻る |
| 3 | 孫ノードだけを反転し、他ノードは変化しない | 孫のみ反転、親は不変 |
| 4 | 存在しない ID を渡してもツリーは変化しない | ツリーに変化なし |

---

### 1-2. `renameNode` — ノード名の変更（4 件）

| # | テスト名 | 期待結果 |
|---|---|---|
| 5 | ノード名を変更する | `name` が新しい値になる |
| 6 | 名前以外のフィールドは変化しない | `expanded`、`summary` は不変 |
| 7 | 孫ノードをリネームできる | 孫ノードの名前が変わる |
| 8 | 存在しない ID はツリーに影響しない | 既存ノードの名前は変化しない |

---

### 1-3. `addChildNode` — 子ノードの追加（5 件）

| # | テスト名 | 期待結果 |
|---|---|---|
| 9 | `__root__` に追加するとルート直下に新ノードが追加される | `children` の長さが 1 になる |
| 10 | 追加されたノードは正しい初期値を持つ | `expanded: true`、`sessions: {claude:null, codex:null, gemini:null, copilot:null, perplexity:null}`、`urls: []` |
| 11 | 既存ノードに子ノードを追加するとそのノードの children に入る | 親の `children` が 1 件になる |
| 12 | 子ノードを追加すると親ノードが `expanded: true` になる | 親の `expanded` が `true` になる |
| 13 | 複数回追加しても既存ノードは保持される | 追加順に `children` に並ぶ |

---

### 1-4. `deleteNode` — ノードの削除（4 件）

| # | テスト名 | 期待結果 |
|---|---|---|
| 14 | ルート直下ノードを削除できる | 指定ノードが消え、兄弟は残る |
| 15 | 孫ノードを削除しても親は残る | 親は残り、`children` が空になる |
| 16 | 子ノードを持つノードを削除するとその配下も全て消える | 指定ノード以下が全て消える |
| 17 | 存在しない ID を渡してもツリーは変化しない | `children` の長さは不変 |

---

### 1-5. `setSessionId` — セッション ID / URL の設定（6 件）

| # | テスト名 | 期待結果 |
|---|---|---|
| 18 | claude のセッション ID をセットできる | `sessions.claude` が新しい値になる |
| 19 | claude だけが変わり codex/gemini は変化しない | `codex`、`gemini` は不変 |
| 20 | 孫ノードにセッション ID をセットできる | 孫の `sessions.gemini` が更新される |
| 21 | copilot の URL をセットできる | `sessions.copilot` に URL が保存される |
| 22 | perplexity の URL をセットできる | `sessions.perplexity` に URL が保存される |
| 23 | copilot をセットしても他のセッションは変化しない | `claude`、`codex`、`gemini`、`perplexity` は不変 |

---

### 1-6. `getNodePath` — ノードのパス取得（5 件）

| # | テスト名 | 期待結果 |
|---|---|---|
| 24 | ルート直下ノードのパスは `[ノード名]` | `['認証方式']` |
| 25 | 深さ3の孫ノードのパスは `[親名, 子名, 孫名]` | `['親', '子', '孫']` |
| 26 | 存在しない ID は空配列を返す | `[]` |
| 27 | 同名ノードが複数あっても ID で正しく区別する | 各ノードの正しいパスが返る |
| 28 | 同名の親と子があるとき子のパスは `[親名, 子名]` | `['設計', '設計']` |

---

### 1-7. `isDescendant` — 子孫判定（4 件）

| # | テスト名 | 期待結果 |
|---|---|---|
| 29 | 同一ノードは `true` を返す | `true` |
| 30 | 子孫ノードは `true` を返す | `true` |
| 31 | 祖先ノードは `false` を返す | `false` |
| 32 | 無関係ノードは `false` を返す | `false` |

---

### 1-8. `moveNode` — ノードの移動（6 件）

| # | テスト名 | 期待結果 |
|---|---|---|
| 33 | ノードを別の親の子に移動できる | 元の親から消え、新しい親の `children` に入る |
| 34 | ノードをルート直下に移動できる | `tree.children` に追加され、元の親からは消える |
| 35 | 自分自身への移動は変更なし | 同一ツリーオブジェクトが返る |
| 36 | 子孫への移動（循環）は変更なし | 同一ツリーオブジェクトが返る |
| 37 | 移動後も他のノードは保持される | 既存ノードの構造は維持される |
| 38 | 存在しない nodeId は変更なし | 同一ツリーオブジェクトが返る |

---

## 2. IPCテスト — `tests/ipc/fileOps.test.ts`

対象: `electron/fileOps.ts`（ファイルシステム操作）  
環境: Node.js（実ファイルシステム、tmp ディレクトリを使用）  
件数: **26 件**

---

### 2-1. `ensureFolderPath` — ネストフォルダの作成（4 件）

| # | テスト名 | 期待結果 |
|---|---|---|
| 1 | 存在しないネストパスを作成する | `tmpDir/認証方式/JWT調査/` が作成される |
| 2 | 既存フォルダを指定してもエラーにならない | `success: true` が返る |
| 3 | ファイル名に使えない文字は `_` に置換される | `tmpDir/bad_name_here/` が作成される |
| 4 | 単一セグメントでも動作する | `tmpDir/single/` が作成される |

---

### 2-2. `removeFolderPath` — フォルダの再帰削除（3 件）

| # | テスト名 | 期待結果 |
|---|---|---|
| 5 | フォルダとその中身を再帰削除する | `parent/` 以下が全て削除される |
| 6 | 存在しないパスでもエラーにならない | `success: true` が返る |
| 7 | 削除後、兄弟フォルダは残る | `keep/` は残る |

---

### 2-3. `addRecentProject` / `readRecentProjects` — 最近のプロジェクト履歴（6 件）

| # | テスト名 | 期待結果 |
|---|---|---|
| 8 | 新規追加するとリストの先頭に入る | `list[0].path === '/proj/A'` |
| 9 | 同じパスを再追加すると重複せず先頭に移動する | リスト長 2、`list[0]` が A |
| 10 | 17 件追加すると 16 件に切り詰められる | リスト長が 16 になる |
| 11 | `openedAt` が ISO 8601 形式で記録される | 有効な ISO 8601 文字列 |
| 12 | ファイルが存在しないとき空配列を返す | `[]` |
| 13 | 不正 JSON でもクラッシュせず空配列を返す | `[]`（例外なし） |

---

### 2-4. `createProject` — プロジェクトの新規作成（3 件）

| # | テスト名 | 期待結果 |
|---|---|---|
| 14 | 新規フォルダを作成し tree.json を初期化する | `tree.json` が `{name:'MyProject', children:[]}` で作成される |
| 15 | 既存フォルダを指定しても tree.json が上書きされない | 手動で書き換えた内容が保持される |
| 16 | プロジェクト名に禁止文字が含まれる場合 `_` に置換される | `bad_name/` フォルダが作成される |

---

### 2-5. `readTree` / `writeTree` — tree.json の読み書き（3 件）

| # | テスト名 | 期待結果 |
|---|---|---|
| 17 | `writeTree` で書き込んだデータを `readTree` で読み返せる | 書き込んだオブジェクトと同一 |
| 18 | `tree.json` が存在しないとき `readTree` は `null` を返す | `null` |
| 19 | `tree.json` が不正 JSON のとき `readTree` は `null` を返す | `null`（例外なし） |

---

### 2-6. `readLayout` / `writeLayout` — layout.json の読み書き（5 件）

| # | テスト名 | 期待結果 |
|---|---|---|
| 20 | `writeLayout` で書き込んだデータを `readLayout` で読み返せる | 書き込んだオブジェクトと同一 |
| 21 | `layout.json` が存在しないとき空オブジェクトを返す | `{}` |
| 22 | `layout.json` が不正 JSON のとき空オブジェクトを返す | `{}`（例外なし） |
| 23 | 空のレイアウトを書き込んで読み返せる | `{}` |
| 24 | 上書き保存すると最新の値が反映される | 最後に書いた座標が返る |

---

### 2-7. `createFolder` — フォルダの作成（2 件）

| # | テスト名 | 期待結果 |
|---|---|---|
| 25 | 指定された名前のフォルダを作成する | `tmpDir/newFolder/` が作成される |
| 26 | 既存フォルダでもエラーにならない | `success: true` が返る |

---

### 2-8. `writeNodeMarkdown` — README.md の自動生成（9 件）

| # | テスト名 | 期待結果 |
|---|---|---|
| 28 | summary と子ノードがある場合、README.md を正しく生成する | `# 見出し`・`## Summary`・`## Sub-topics` が生成される |
| 29 | summary がない場合、Summary セクションは含まれない | `## Summary` なし |
| 30 | 子ノードがない場合、Sub-topics セクションは含まれない | `## Sub-topics` なし |
| 31 | summary も子ノードもない場合、ヘッダーのみ | `# Empty` のみ |
| 32 | フォルダが存在しなくても自動作成される | 中間フォルダごと作成される |
| 33 | ノード名に禁止文字が含まれるとき `_` に置換してフォルダが作られる | `bad_name_here/README.md` が存在する |
| 34 | パストラバーサル（`..` 含むセグメント）を検出して失敗を返す | `success: false`・`Path traversal` エラー |
| 35 | 上書き保存すると最新の内容に更新される | 前の内容が消え新しい内容になる |
| 36 | summary の前後の空白はトリムされる | トリム済みの文字列が保存される |

---

## 3. その他ユニットテスト

### 3-0. `tests/unit/mindmapHelpers.test.ts`（26 件）

対象: `src/lib/mindmapHelpers.ts`（`toMarkmapNode`・`findNodeById`・payload ID 抽出ロジック）

#### `toMarkmapNode` — マインドマップノード変換（14 件）

| # | テスト名 | 期待結果 |
|---|---|---|
| 1 | セッションもターミナルもなければノード名だけが content | `content === 'テスト'` |
| 2 | payload に id と name が含まれる | `payload.id`・`payload.name` が正しい |
| 3 | アクティブなターミナルがあると tool 名バッジが含まれる | `[claude]` が content に含まれる |
| 4 | 別ノードのターミナルはバッジに影響しない | content はノード名のみ |
| 5 | claude セッション | `>C<` バッジが表示される |
| 6 | codex セッション | `>X<` バッジが表示される |
| 7 | gemini セッション | `>G<` バッジが表示される |
| 8 | copilot セッション | `>Co<` バッジが表示される |
| 9 | perplexity セッション | `>P<` バッジが表示される |
| 10 | 全セッション設定 | 全バッジが含まれる |
| 11 | バッジあり | `<small>` タグで囲まれる |
| 12 | 子なし | `children` が `undefined` |
| 13 | 子あり | `children` 配列が生成される |
| 14 | 孫ノード | 再帰的に変換される |

#### `findNodeById` — ID によるノード検索（6 件）

| # | テスト名 | 期待結果 |
|---|---|---|
| 15 | ルート直下のノードを見つける | 正しい `TreeNode` が返る |
| 16 | 子ノードを見つける | 正しい `TreeNode` が返る |
| 17 | 孫ノードを見つける | 正しい `TreeNode` が返る |
| 18 | 存在しない ID | `null` を返す |
| 19 | 空ツリー | `null` を返す |
| 20 | 同名ノードが複数 | ID で正しく区別する |

#### markmap payload ID 抽出ロジック（6 件）

DOM の `__data__` バインディングを模擬して `handleContextMenu` / `handleClick` の分岐ロジックを検証する。

| # | テスト名 | 期待結果 |
|---|---|---|
| 21 | `payload.id` が文字列 | その値を返す |
| 22 | `payload` が undefined | `undefined` を返す |
| 23 | `data` 自体が undefined | `undefined` を返す |
| 24 | `payload.id === '__root__'` | root-node 扱いと判定される |
| 25 | 通常ノード ID | root-node 扱いにならない |
| 26 | `toMarkmapNode` 生成データ | `extractNodeId` で正しく ID 取得できる |

---

### 3-1. `tests/unit/paneToggle.test.ts`（23 件）

左右ペインのトグルボタン（表示・非表示切り替え）のロジックテスト。

| テスト内容 |
|---|
| 左ペイン初期状態は表示 |
| 左ペイントグルボタンで非表示に切り替わる |
| 非表示状態で再クリックすると表示に戻る |
| 右ペインのトグル動作（同上） |
| 左右独立してトグル可能 |
| 左非表示時に幅が 0 になる、など |

---

### 3-2. `tests/unit/paneResize.test.ts`（24 件）

左右ペインのドラッグリサイズのロジックテスト。

| テスト内容 |
|---|
| 左ペインの幅は 0〜480px にクランプされる |
| ドラッグ距離に応じて幅が変化する |
| 右ペインのリサイズ（同上） |
| 最小幅 0 以下にならない |
| 最大幅 480 以上にならない |
| 複数回ドラッグしても値が正しく累積する、など |

---

### 3-3. `tests/unit/terminalMode.test.ts`（23 件）

TerminalMode（tab / float / hidden）の状態遷移テスト。

| テスト内容 |
|---|
| CLI 起動時にモードが `tab` になる |
| `×` ボタンでモードが `hidden` になる |
| `↗` ボタンでモードが `float` になる |
| フローティングから `⬇` でモードが `tab` に戻る |
| 非表示ターミナルで同じ CLI を再起動するとモードが `tab` に戻る |
| pty 終了時にターミナルエントリが削除される、など |

---

### 3-4. `tests/unit/launchFlow.test.ts`（13 件）

CLI 起動フロー（`launchCLI` 関数）のロジックテスト。

| テスト内容 |
|---|
| Claude 初回起動のコマンドは `'claude'` |
| Claude セッション再開のコマンドは `claude --resume <id>` |
| Codex 起動のコマンドは `'codex'` |
| Gemini 起動のコマンドは `'gemini'` |
| 同じ nodeId・tool が既存の場合は再起動せずアクティブ化のみ、など |

---

### 3-5. `tests/unit/terminalBuffer.test.ts`（8 件）

ターミナルバッファ（出力キャッシュ）のロジックテスト。

| テスト内容 |
|---|
| バッファに出力が蓄積される |
| バッファが 100KB を超えた場合に先頭を切り捨てる |
| pty 終了時にバッファがクリアされる、など |

---

## 4. E2Eテスト — `tests/e2e/`

対象: Electron アプリ全体（実機起動）  
環境: Playwright + Electron（ビルド済みアプリを起動）  
件数: **17 件**

> **前提**: `npm run build` でビルド済みであること。  
> ダイアログはテスト内で `dialog.showOpenDialog` をモックして任意のパスを返す。

---

### 4-1. プロジェクト操作 — `project.spec.ts`（5 件）

| # | テスト名 | 操作手順 | 期待結果 |
|---|---|---|---|
| 1 | アプリ起動時に「プロジェクトなし」画面が表示される | アプリ起動 | 「プロジェクトなし」テキストが表示される |
| 2 | 「Open Project」でフォルダを選択するとプロジェクト名が表示される | ボタンクリック → tmpDir 選択 | プロジェクト名が表示される |
| 3 | プロジェクトを開くと `tree.json` が作成される | プロジェクトを開く | `<tmpDir>/tree.json` が存在する |
| 4 | 同じプロジェクトを 2 回開いても最近リストに重複しない | 同一プロジェクトを開く → 再起動 | 一覧が 1 件のみ |
| 5 | 最近のプロジェクト一覧をクリックするとそのプロジェクトが開く | 一覧をクリック | プロジェクト名が表示される |

---

### 4-2. マインドマップ操作 — `mindmap.spec.ts`（6 件）

| # | テスト名 | 操作手順 | 期待結果 |
|---|---|---|---|
| 6 | プロジェクトを開くとマインドマップにルートノードが表示される | プロジェクトを開く | プロジェクト名がノードに表示される |
| 7 | ルートノードを右クリックすると「Add child node」メニューが表示される | ルートノード右クリック | メニュー項目が表示される |
| 8 | 子ノードを追加するとマインドマップに新ノードが表示される | 右クリック → 子追加 → 入力 → Enter | 新ノードがマップに表示される |
| 9 | 子ノードを追加するとプロジェクトフォルダにサブフォルダが作成される | 「DB設計」ノードを追加 | `<projectRoot>/DB設計/` が作成される |
| 10 | 孫ノード追加でネストフォルダが作成される | 「認証」の子として「JWT調査」を追加 | `<projectRoot>/認証/JWT調査/` が作成される |
| 11 | ノードを削除すると対応フォルダも削除される | ノード右クリック → 削除 → 確認 | ノードとフォルダが共に削除される |

---

### 4-3. ファイルエクスプローラー — `fileExplorer.spec.ts`（6 件）

| # | テスト名 | 操作手順 | 期待結果 |
|---|---|---|---|
| 12 | プロジェクトを開くと右ペインにフォルダツリーが表示される | プロジェクトを開く | ファイルエクスプローラーが表示される |
| 13 | フォルダをクリックすると展開される | フォルダをクリック | 子フォルダが表示される |
| 14 | 展開済みフォルダを再クリックすると折りたたまれる | 展開後に再クリック | 子フォルダが非表示になる |
| 15 | 右クリック「New folder」でインライン入力が表示される | 右クリック → New folder | インライン入力欄が表示される |
| 16 | フォルダ名を入力して Enter するとフォルダが作成される | 入力後 Enter | フォルダが作成され、リストに表示される |
| 17 | 右クリック「Show in Explorer」が選択できる | フォルダ右クリック | メニュー項目が表示される |

---

## 5. テスト集計

| カテゴリ | ファイル | 件数 |
|---|---|---|
| ユニット | `tests/unit/treeOps.test.ts` | 38 件 |
| ユニット | `tests/unit/mindmapHelpers.test.ts` | 26 件 |
| ユニット | `tests/unit/paneToggle.test.ts` | 23 件 |
| ユニット | `tests/unit/paneResize.test.ts` | 24 件 |
| ユニット | `tests/unit/terminalMode.test.ts` | 23 件 |
| ユニット | `tests/unit/launchFlow.test.ts` | 13 件 |
| ユニット | `tests/unit/terminalBuffer.test.ts` | 8 件 |
| IPC | `tests/ipc/fileOps.test.ts` | 35 件 |
| E2E | `tests/e2e/project.spec.ts` | 5 件 |
| E2E | `tests/e2e/mindmap.spec.ts` | 6 件 |
| E2E | `tests/e2e/fileExplorer.spec.ts` | 6 件 |
| **合計** | | **207 件** |
